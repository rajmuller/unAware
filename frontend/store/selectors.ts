export const selectUser = (state) => state.user;

export const selectPerformer = (state) => state.performer;

export const selectRecommended = (state) => state.recommendations;

export const selectFavorites = (state) => state.favorites;

export const selectDevToolsOpen = (state) => state.toolsOpen;

export const selectThemeName = (state) => state.theme;
